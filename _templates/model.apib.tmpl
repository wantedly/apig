# Group {{ pluralize .Model.Name }}
Welcome to the {{ pluralize (toSnakeCase .Model.Name) }} API. This API provides access to the {{ pluralize (toSnakeCase .Model.Name) }} service.

## {{ pluralize (toSnakeCase .Model.Name) }} [/{{ pluralize (toSnakeCase .Model.Name) }}]

### Create {{ toSnakeCase .Model.Name }} [POST]

Create a new {{ toSnakeCase .Model.Name }}

+ Request {{ toSnakeCase .Model.Name }} (application/json; charset=utf-8)
    + Headers

            Accept: application/vnd.{{ .User }}+json
    + Attributes
{{ range (requestParams .Model.Fields) }}
        + {{ .JSONName }}{{ if (apibDefaultValue .) ne "" }}: {{ apibDefaultValue . }}{{ end }} ({{ apibType . }}){{ end }}

+ Response 201 (application/json; charset=utf-8)
    + Attributes ({{ toSnakeCase .Model.Name }}, fixed)

### Get {{ pluralize (toSnakeCase .Model.Name) }} [GET]

Returns {{ article (toSnakeCase .Model.Name) }} list.

+ Request (application/json; charset=utf-8)
    + Headers

            Accept: application/vnd.{{ .User }}+json

+ Response 200 (application/json; charset=utf-8)
    + Attributes (array, fixed)
        + ({{ toSnakeCase .Model.Name }})

## {{ toSnakeCase .Model.Name }} details [/{{ pluralize (toSnakeCase .Model.Name) }}/{id}]

+ Parameters
    + id: `1` (enum[string]) - The ID of the desired {{ toSnakeCase .Model.Name }}.
        + Members
            + `1`
            + `2`
            + `3`

### Get {{ toSnakeCase .Model.Name }} [GET]

Returns {{ article (toSnakeCase .Model.Name) }}.

+ Request (application/json; charset=utf-8)
    + Headers

            Accept: application/vnd.{{ .User }}+json

+ Response 200 (application/json; charset=utf-8)
    + Attributes ({{ toSnakeCase .Model.Name }}, fixed)

### Update {{ toSnakeCase .Model.Name }} [PUT]

Update {{ article (toSnakeCase .Model.Name) }}.

+ Request {{ toSnakeCase .Model.Name }} (application/json; charset=utf-8)
    + Headers

            Accept: application/vnd.{{ .User }}+json
    + Attributes
{{ range (requestParams .Model.Fields) }}
        + {{ .JSONName }}{{ if (apibDefaultValue .) ne "" }}: {{ apibDefaultValue . }}{{ end }} ({{ apibType . }}){{ end }}

+ Response 200 (application/json; charset=utf-8)
    + Attributes ({{ toSnakeCase .Model.Name }}, fixed)

### Delete {{ toSnakeCase .Model.Name }} [DELETE]

Delete {{ article (toSnakeCase .Model.Name) }}.

+ Request (application/json; charset=utf-8)
    + Headers

            Accept: application/vnd.{{ .User }}+json

+ Response 204

# Data Structures
## {{ toSnakeCase .Model.Name }} (object)
{{ range $key, $value := .Model.Fields }}
+ {{ .JSONName }}{{ if (apibDefaultValue .) ne "" }}: {{ apibDefaultValue . }}{{ end }} ({{ apibType . }}){{ end }}
